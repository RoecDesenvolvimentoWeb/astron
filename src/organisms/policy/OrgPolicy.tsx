import { MolTermsAndPolicy } from "@mols/panel/MolTermsAndPolicy";
import { ReactElement } from "react";

export const OrgPolicy = (): ReactElement => {
    return (
        <MolTermsAndPolicy
            className={"h-full w-full"}
        />
    )
}